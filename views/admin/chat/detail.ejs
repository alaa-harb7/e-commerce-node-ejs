<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Chat with <%= targetUser.name %> - Admin</title>
    <link rel="stylesheet" href="/css/topico-style.css">
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700&display=swap" rel="stylesheet">
    <style>
        .chat-container { max-width: 800px; margin: 20px auto; height: 80vh; display: flex; flex-direction: column; background: #fff; border-radius: 12px; box-shadow: 0 2px 20px rgba(0,0,0,0.1); }
        .chat-header { padding: 20px; border-bottom: 1px solid #eee; font-weight: 600; background: #f8f9fa; border-radius: 12px 12px 0 0; }
        .chat-messages { flex: 1; padding: 20px; overflow-y: auto; display: flex; flex-direction: column; gap: 15px; }
        .message { max-width: 70%; padding: 10px 15px; border-radius: 15px; font-size: 0.95em; line-height: 1.4; }
        .message.received { align-self: flex-start; background: #f1f0f0; border-bottom-left-radius: 2px; }
        .message.sent { align-self: flex-end; background: #000; color: #fff; border-bottom-right-radius: 2px; }
        .chat-input-area { padding: 20px; border-top: 1px solid #eee; display: flex; gap: 10px; }
        .chat-input { flex: 1; padding: 12px; border: 1px solid #ddd; border-radius: 25px; outline: none; }
        .send-btn { background: #000; color: #fff; border: none; padding: 0 25px; border-radius: 25px; cursor: pointer; font-weight: 600; }
        .message-time { font-size: 0.7em; opacity: 0.7; margin-top: 5px; display: block; text-align: right; }
    </style>
</head>
<body>
    <%- include('../../partials/topico-header') %>

    <div class="chat-container">
        <div class="chat-header">
            Chat with <%= targetUser.name %>
        </div>
        <div class="chat-messages" id="messages">
            <% messages.forEach(msg => { %>
                <div class="message <%= msg.role === 'admin' ? 'sent' : 'received' %>">
                    <div><%= msg.content %></div>
                    <span class="message-time"><%= new Date(msg.createdAt).toLocaleTimeString([], {hour: '2-digit', minute:'2-digit'}) %></span>
                </div>
            <% }); %>
        </div>
        <form class="chat-input-area" id="chatForm">
            <input type="text" class="chat-input" id="messageInput" placeholder="Type a message..." autocomplete="off">
            <button type="submit" class="send-btn">Send</button>
        </form>
    </div>

    <script src="/socket.io/socket.io.js"></script>
    <script>
        // Use Global Socket if available
        const socket = window.appSocket || io({
            withCredentials: true,
            transports: ['websocket', 'polling'] 
        });
        
        const chatForm = document.getElementById('chatForm');
        const input = document.getElementById('messageInput');
        const messagesDiv = document.getElementById('messages');
        const targetUserId = '<%= targetUser._id %>';

        // Join this user's chat room
        socket.emit('admin_join_chat', targetUserId);

        // Scroll to bottom
        messagesDiv.scrollTop = messagesDiv.scrollHeight;

        chatForm.addEventListener('submit', (e) => {
            e.preventDefault();
            if (input.value) {
                socket.emit('sendMessage', { content: input.value, targetUserId: targetUserId });
                input.value = '';
            }
        });

        socket.on('newMessage', (msg) => {
            const div = document.createElement('div');
            // Check if I sent it (role admin) or User sent it
            const type = msg.sender.role === 'admin' ? 'sent' : 'received';
            div.className = `message ${type}`;
            div.innerHTML = `
                <div>${msg.content}</div>
                <span class="message-time">${new Date(msg.createdAt).toLocaleTimeString([], {hour: '2-digit', minute:'2-digit'})}</span>
            `;
            messagesDiv.appendChild(div);
            messagesDiv.scrollTop = messagesDiv.scrollHeight;
        });
    </script>
</body>
</html>
