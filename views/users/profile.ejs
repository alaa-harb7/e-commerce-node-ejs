<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>My Profile - TOPICO</title>
    <link rel="stylesheet" href="/css/topico-style.css">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700&display=swap" rel="stylesheet">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <style>
                .main-nav {
            background-color: #ffffff;
            border-bottom: 1px solid #e5e5e5;
            margin-bottom: 20px;
        }
        .nav-container {
            max-width: 1400px;
            margin: 0 auto;
            padding: 0 20px;
            display: flex;
            align-items: center;
            gap: 30px;
            height: 50px;
        }
        .nav-link {
            text-decoration: none;
            padding: 15px 25px;
            letter-spacing: 0.5px;
            color: #333;
            font-weight: 500;
            font-size: 14px;
            text-transform: uppercase;
            height: 50px;
            display: flex;
            align-items: center;
            position: relative;
            transition: color 0.3s;
        }
        .nav-link:hover, .nav-link.active {
            color: #FFD700;
        }
        .nav-link.active::after {
            content: '';
            position: absolute;
            bottom: -1px;
            left: 0;
            width: 100%;
            height: 2px;
            background-color: #FFD700;
        }
    </style>
</head>
<body>
    <%- include('../partials/topico-header') %>

    <nav class="main-nav">
        <div class="nav-container">
            <a href="/" class="nav-link">HOME</a>
            <a href="/products?showAll=true" class="nav-link">ALL PRODUCTS</a>
            <a href="/about" class="nav-link">ABOUT US</a>
            <a href="/contact" class="nav-link">CONTACT</a>
            <a href="/categories" class="nav-link">CATEGORIES</a>
            <a href="/subcategories" class="nav-link">SUBCATEGORIES</a>
            <a href="/brands" class="nav-link">BRANDS</a>
            <% if (typeof user !== 'undefined' && user && user.role === 'admin') { %>
                <a href="/users" class="nav-link">USERS</a>
            <% } %>
        </div>
    </nav>

    <style>
        .profile-container {
            max-width: 800px;
            margin: 40px auto;
        }
        .profile-card {
            background: #ffffff;
            border-radius: 12px;
            box-shadow: 0 5px 25px rgba(0,0,0,0.05);
            border: 1px solid #eaeaea;
            overflow: hidden;
        }
        .profile-header {
            background: #ffffff;
            padding: 30px;
            text-align: center;
            border-bottom: 1px solid #f0f0f0;
            position: relative;
        }
        .profile-title {
            font-size: 24px;
            font-weight: 700;
            color: #1a1a1a;
            margin: 0;
        }
        .profile-body {
            padding: 40px;
        }
        .avatar-container {
            width: 120px;
            height: 120px;
            margin: 0 auto 30px;
            position: relative;
        }
        .user-avatar {
            width: 100%;
            height: 100%;
            border-radius: 50%;
            object-fit: cover;
            border: 3px solid #FFD700;
            padding: 3px;
            background: #fff;
        }
        .avatar-placeholder {
            width: 100%;
            height: 100%;
            border-radius: 50%;
            background: #f5f5f5;
            display: flex;
            align-items: center;
            justify-content: center;
            color: #ddd;
            font-size: 50px;
            border: 3px solid #eee;
        }
        .info-grid {
            display: grid;
            grid-template-columns: 1fr 1fr;
            gap: 30px;
            margin-bottom: 40px;
        }
        .info-item {
            display: flex;
            flex-direction: column;
        }
        .info-label {
            font-size: 12px;
            text-transform: uppercase;
            letter-spacing: 1px;
            color: #888;
            margin-bottom: 8px;
            font-weight: 600;
        }
        .info-value {
            font-size: 16px;
            color: #222;
            font-weight: 500;
            border-bottom: 1px solid #eee;
            padding-bottom: 10px;
        }
        .role-badge {
            display: inline-block;
            background: #000;
            color: #FFD700;
            padding: 4px 12px;
            border-radius: 20px;
            font-size: 12px;
            font-weight: 700;
            text-transform: uppercase;
            letter-spacing: 0.5px;
        }
        .user-badge {
            background: #f0f0f0;
            color: #666;
        }
        .action-buttons {
            display: flex;
            justify-content: center;
            gap: 20px;
        }
        .btn-profile {
            padding: 12px 30px;
            border-radius: 6px;
            font-weight: 600;
            font-size: 14px;
            text-decoration: none;
            transition: all 0.3s ease;
            display: inline-flex;
            align-items: center;
            gap: 10px;
            border: none;
        }
        .btn-edit {
            background-color: #000;
            color: #fff;
        }
        .btn-edit:hover {
            background-color: #333;
            color: #fff;
            transform: translateY(-2px);
        }
        .btn-password {
            background-color: #FFD700;
            color: #000;
        }
        .btn-password:hover {
            background-color: #FFC700;
            color: #000;
            transform: translateY(-2px);
        }
        @media (max-width: 600px) {
            .info-grid { grid-template-columns: 1fr; gap: 20px; }
            .action-buttons { flex-direction: column; }
            .btn-profile { width: 100%; justify-content: center; }
        }
    </style>

    <main class="profile-container">
        <div class="profile-card">
            <div class="profile-header">
                <h1 class="profile-title">My Profile</h1>
            </div>
            <div class="profile-body">
                <div class="avatar-container">
                    <% if (typeof data !== 'undefined' && data.profileImage) { %>
                        <img src="<%= data.profileImage %>" alt="Profile" class="user-avatar">
                    <% } else { %>
                        <div class="avatar-placeholder">
                            <i class="fas fa-user"></i>
                        </div>
                    <% } %>
                </div>

                <div class="info-grid">
                    <div class="info-item">
                        <span class="info-label">Full Name</span>
                        <div class="info-value"><%= typeof data !== 'undefined' ? data.name : 'N/A' %></div>
                    </div>
                    <div class="info-item">
                        <span class="info-label">Email Address</span>
                        <div class="info-value"><%= typeof data !== 'undefined' ? data.email : 'N/A' %></div>
                    </div>
                    <div class="info-item">
                        <span class="info-label">Phone Number</span>
                        <div class="info-value"><%= typeof data !== 'undefined' && data.phone ? data.phone : 'Not provided' %></div>
                    </div>
                    <div class="info-item">
                        <span class="info-label">Account Role</span>
                        <div style="margin-top: 5px;">
                            <span class="role-badge <%= typeof data !== 'undefined' && data.role === 'user' ? 'user-badge' : '' %>">
                                <%= typeof data !== 'undefined' ? data.role : 'user' %>
                            </span>
                        </div>
                    </div>
                </div>

                <div class="action-buttons">
                    <a href="/users/me/edit" class="btn-profile btn-edit">
                        <i class="fas fa-pen"></i> Edit Profile
                    </a>
                    <a href="/users/changeMyPassword" class="btn-profile btn-password">
                        <i class="fas fa-lock"></i> Change Password
                    </a>
                </div>
            </div>
        </div>
    </main>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>




